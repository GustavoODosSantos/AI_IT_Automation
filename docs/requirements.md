# Requirements

## Python Version
- Python 3.9 or higher (tested on Python 3.13.7)

## Libraries
- **psutil**
  - Install with:
    ```bash
    pip install psutil
    ```
- Standard Python libraries (already included):
  - os
  - platform
  - socket
  - json
  - time
  - datetime
